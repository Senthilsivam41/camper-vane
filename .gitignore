# Binaries and DB
*.db
*.db-shm
*.db-wal
camper_vane.db
/server

# Local secrets (keep `.env.example` committed; never commit real `.env`)
.env
.env.local
.env.*.local

# Dependency directories
node_modules/
dist/

# Agent & AutoClaw metadata
.autoclaw/
.agent/
.claude/
.gemini/

# Codegraph local index
.codegraph/
